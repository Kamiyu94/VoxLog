# 更新紀錄 Changelog

本檔記錄 VoxLog 對使用者有感的變動。技術細節見各 commit。

## 2026-06-12

這一輪原本想探索「用雲端 AI 做語音辨識」，實測後決定**回歸地端**，但過程中把地端流程
與整體介面大幅改好了。

### ✨ 地端辨識更準

- **新增「關鍵術語 / 人名」欄位** — 開會前填入客戶名、與會者、產品/系統、案號（用逗號
  分隔），會**直接提示辨識模型認對專有名詞**，不必等事後校正。旁邊即時顯示剩餘額度。
- **新增「會議背景」欄位** — 給第二步驟的 AI 校正/摘要參考。
- **轉錄模型依電腦記憶體自動建議** — 32GB 機器預設 `large-v3`（最準）、8GB 預設 `small`
  （不會爆記憶體），並記住你上次的選擇。
- **辨識引擎改為下拉選單** — Whisper / WhisperX 切換更清楚，相關設定（HF Token、說話人數）
  只在需要時出現。

### 🎬 介面改善

- **YouTube 下載改為「點按鈕跳出對話框」輸入網址** — 省掉主畫面兩行輸入列；Cookies 收為
  進階選項（一般公開影片免填）。
- **視窗縮小、版面更貼合** — 「開始轉錄 / 停止」改成右側上下排列，不再被擠出畫面；Log
  區會隨視窗放大而變清楚。

### 🔬 探索與決策

- **探索雲端語音辨識後決定移除** — 雲端對「多人會議」無法穩定分辨「誰在說話」（沒有聲紋
  錨定），且難搞音檔易出錯，故移除，辨識一律走地端。完整理由與「未來何時值得重啟」記於
  `docs/cloud-stt-exploration.md`。第二步驟的雲端 AI 校正/摘要不受影響、照舊可用。

## 2026-06-10

針對同事在 MacBook Air M2（8GB）實際安裝、使用時踩到的問題做的一輪修正與體驗改善。
更新方式見 `MACOS_SETUP.html` / `WINDOWS_SETUP.html` 的「🔄 之後怎麼更新到最新版」。

### 🐛 修正

- **自動清理 Whisper 幻覺重複** — Whisper 偶爾會在附和聲或靜音處卡進重複迴圈，吐出
  「對對對對…」幾百個字。現在轉錄時會自動壓掉這類重複（單字連發、逗號分隔重複、
  整句重複），保留正常的「謝謝」「哈哈哈」。Whisper 與 WhisperX 都套用。

### ✨ 摘要相關

- **摘要 prompt 強化「保留具體事實」** — 新增「關鍵事實（Key Facts）」區塊（人物角色、
  時程期限、數字金額、系統技術、案號/檔案位置），並要求摘要不可把人名/日期/數字/系統名
  等落地細節抽象化或漏掉，轉述原則時不可簡化到語意相反。減少摘要「好看但漏重點」的情況。

- **第二分頁新增「補充說明」欄位** — 看完第一版逐字稿、發現廠商/產品/人名被聽錯時，
  可在「逐字稿處理」分頁填正確資訊，**校正與摘要都會參考**。會自動與第一頁的轉錄情境
  合併（預設的轉錄提示不會污染 AI）。

- **摘要寫回「會議時間」** — 重新加上「基本資訊／會議時間」：優先從錄音檔名判讀
  （如「6月10日 14-56」），判讀不到就用檔案的修改時間當會議時間。
- **摘要標題＝錄音檔名** — 文件大標題與輸出檔名直接沿用錄音檔名（去掉 `_transcript`
  等後綴），不再由 AI 自行生標題，檔名也更乾淨（`xxx_摘要.docx`）。

### 🐛 其他修正

- **貼上 API Key 不再出錯（macOS）** — Key 欄位是星號遮罩，看不到內容；舊版在 Mac 上
  Cmd+A 全選不可靠，貼上會把舊殘值黏在新 key 後面變成壞字串，導致 Gemini 一直回 401。
  現在遮罩欄位（API Key / Token）**貼上＝整段取代，並自動去除前後空白/換行**，直接貼就會通，
  不必再手動編 `config.json`。Cmd+A 全選也改成可靠的手動版。
  （Windows 原生 Ctrl+A/V 正常，本來就沒這問題。）
  commit `100bca2`

- **8GB 機器一跑就閃退** — 預設轉錄模型從 `medium`（約 5GB）改為 `small`（約 2GB），
  記憶體小的電腦不會一開箱就爆。要更高精度可在介面手動選 `medium` / `large`。
  低階機器並建議用 **Whisper**（非 WhisperX，後者另載對齊與分離模型、更吃記憶體）。
  commit `755eb3c`

### 🎨 介面

- **Gemini 模型下拉標註免費／付費** — 列出常用的 Gemini 模型，並在每個後面標「免費」或
  「付費」（Pro 系列需開啟付費方案，免費方案額度為 0；flash 系列免費可用）。標註只是顯示，
  實際送出的仍是純模型 ID（避免標註被當成模型名導致 404）。
- **加上右鍵選單(剪下/複製/貼上/全選)** — macOS 上 tkinter 的 Cmd+V 在某些焦點時機
  會被系統搶先中斷而失靈（平台老問題，故偶爾復發）。新增右鍵選單作為**永遠可用的後備**，
  且涵蓋所有輸入框（含新的補充說明框、對話框）。貼上一樣套用遮罩欄位「整段取代」的邏輯。

- **改成兩個分頁,操作順序一看就懂** — 主畫面拆成「①  轉錄逐字稿」與
  「②  逐字稿處理（AI 校正 / 摘要 / 字幕檔）」兩個分頁,分頁按鈕放大、各自頂部有一行
  較大字的步驟提示;重複的標籤改名（「轉錄模型 / 轉錄引擎」對比「AI 引擎 / AI 模型」),
  不再混淆。**轉錄完成會自動跳到「逐字稿處理」分頁**帶使用者進下一步。狀態列、進度條、
  計時器、Log 固定在底部常駐(Log 縮小、不再吃版面),兩個分頁都看得到進度。

### ✨ 新功能

- **摘要段落改用台灣會議紀錄慣用語** — 段落名從翻譯腔改成順口的中文：執行摘要→會議總覽、
  重點摘要→會議重點、關鍵事實→會議要素、重點主題→討論內容、決策與原則→決議事項。
  最終結構：基本資訊 / 會議總覽 / 會議重點 / 會議要素 / 討論內容 / 決議事項 / 待辦事項 / 標籤。

- **摘要格式調整（合併第一版的優點）** — 待辦事項改回「負責人/事項/期限」**表格**（原
  「行動項目 + checkbox」改名為更自然的「待辦事項」）；標籤改回**三類**（人物/主題/議題，
  好歸檔搜尋）；新增「重點摘要」3–5 條列(快速掃讀)。保留升級後的執行摘要、關鍵事實、
  分主題敘述與「保留具體事實」強度。

- **摘要品質大升級（對標專業會議紀錄工具）** — 重寫摘要 prompt：產出「執行摘要（一段
  濃縮）＋ 按主題分節的完整敘述 ＋ 依負責人分組、帶 checkbox 與期限的行動項目 ＋ 標籤」。
  docx 輸出同步升級：章節標題加淺灰分隔線、行動項目用 ☐ checkbox、標題與段落間距調整，
  讀起來更接近設計過的摘要文件。**提醒：要這種品質，AI 引擎請選強一點的模型**
  （Gemini Pro 或 Claude Sonnet/Opus，別用最省的 flash-lite）。

- **標說話者時顯示「代表發言」** — WhisperX 辨識多位說話者後，命名對話框過去只丟
  `SPEAKER_00 是誰`，使用者根本無從判斷。現在每位下方會顯示一句**最長的代表發言**
  （自動跳過「嗯」「好的」這類短句），一看就能對上是誰；輸入框加了提示、版面也整理過。

- **完成後自動開檔（Mac / Windows 通用）** — 逐字稿轉錄、AI 校正、產生摘要完成後，
  會自動用系統預設程式打開檔案給使用者過目，不用自己去資料夾找。
  commit `0d63288`

- **摘要輸出改成 Word 檔（.docx）** — 「產生摘要」從 Markdown `_摘要.md` 改為
  `_摘要.docx`，Word / Pages 直接開；標題、項目清單、待辦事項表格、行內粗體都會排好。
  （新增相依套件 `python-docx`，更新後第一次啟動會自動補裝。）
  commit `0d63288`

- **打 `voxlog` 一個字就能啟動（Windows）** — 對齊 Mac 的做法。第一次雙擊 `VoxLog.bat`
  時會自動把 `voxlog.cmd` 註冊進 `%LOCALAPPDATA%\Microsoft\WindowsApps`（預設就在 PATH 上，
  免改 PATH、免管理員權限）。之後在任何終端機打 `voxlog` 即可開（需新開終端機視窗才認得）。
  commit `11ad244`

### 🔧 體驗與相容性

- **Windows 更新會自動補裝新套件** — `VoxLog.bat` 原本「套件只裝第一次」，更新後新套件
  （如 `python-docx`）不會裝、摘要會壞。現在會比對 `requirements.txt`，**有變動就自動補裝**；
  既有安裝下次雙擊即自動補上。
  commit `5080be8`

- **安裝文件補強** — `MACOS_SETUP.html` 更新章節加上「本次新功能」與「更新需重跑
  `pip install -r requirements.txt`」提醒，並把 401 的手動排除標註為「舊版才需要」；
  `WINDOWS_SETUP.html` 新增完整的「更新章節」與 `voxlog` 指令說明。
  commit `9ba5067`、`11ad244`

- **開源發佈準備** — 加入 `LICENSE`（MIT，歡迎自由使用／修改／散布）；README 改寫成對外版
  （功能亮點、截圖位置、快速開始表、連到 CHANGELOG／LICENSE），並修正過時的 MPS 敘述。
